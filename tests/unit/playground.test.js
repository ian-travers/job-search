describe("basic math", () => {
  it("adds two numbers", () => {
    expect(1 + 1).toBe(2);
  });

  it("substact two numbers", () => {
    expect(10 - 8).toBe(2);
    expect(4 - 1).toBe(3);
  });
});
